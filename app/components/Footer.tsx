// Signature shown at the bottom of every page.
import { FOOTER_SIGNATURE } from "@/lib/constants";

export default function Footer() {
  return <footer className="site-footer">{FOOTER_SIGNATURE}</footer>;
}
